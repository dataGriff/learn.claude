---
description: One-paragraph summary of uncommitted changes on this branch
allowed-tools: Bash(git status), Bash(git diff *), Bash(git log *)
---

Below are the current git status and diff for this branch.

## Status

!`git status --short`

## Diff (unstaged + staged)

!`git diff HEAD`

## Recent commits on this branch

!`git log --oneline -10`

---

Write a single paragraph (4-6 sentences) describing what has changed and **why** a reviewer would care. Group related changes. Don't enumerate every file — focus on the intent.
